import 'package:flutter/material.dart';

class HomeListFragment extends StatelessWidget {
  const HomeListFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('ini adalah home')),
    );
  }
}
